
目前工厂项目是华煜和曲晨在做

saip 目前王阳维护
  1，数据预处理，数据标注；
  2，可以选择训练过的模型进行数据标注；不是用的sam，而是训练过的模型；
  3，数据处理完后，模型训练的时候选择多个需要的数据集；针对数据补充的情况；
  4，模型训练完成后，可以设置为下一次训练的初始模型，下一次训练的时候可以选择这个模型，在这个基础上finetune
  5，可以用训练后的模型进行在线的推理；
  后端开发用的rust；
  6，目前嵌入了大模型的功能，正在测试阶段；

戴浩慰
  deepedge 
    通过大模型自动标注，对比相机的结果，然后用来再训练相机的模型；
    美国的项目在试用，美国是徐朔男对接；美国的项目有半年了；
  跑的模型：
    1，基于qwen的8b模型 rexomni，可以进行目标检测；
      效果一般；存在漏报，不如自己训练的模型；
    2，框内用原生 qwen 判断框内是否有需要的目标
      可以判断是否有误识别；需要一个prompt；
      判断框内的目标是什么，然后看框内的目标不是我们需要的目标；

    目前是检测人，检测人的结果不如自己训练的模型；
    如果是不是人的情况，那么有了deepedge后不需要从零开始；
    训练除了用新增的数据，还包含coco数据；会混合训练；

    训练设备和相机在一个局域网中，每个相机间隔一段时间采一个图片；
    相机可以进行分组，每一个组使用一个模型；
    模型推到相机的部分，是 尹盛 对接；

    saip上现在加了一个 ai agent，先标注数据，然后通过大模型自动生成prompt，标注多张后输入大模型可以用来标注；

  高通板子适配：
    模型转成高通板子上能跑的格式，然后嵌入式来做具体推理的部分
    saip使用脚本转成需要的模型；

陆海洋
  soc的适配：导出onnx，导出到板端适配的版本；
    导成不同的格式；caffe / rknn / int4
  模型的量化部署；

  后端的算法部分的c代码，取流，图像的预处理到算法的后处理，
  相机上的前端开发者已经离职了，现在是嵌入式部分负责；后端处理的部分；
  有的soc有些算子不支持，sigmod不支持，在c代码部分手动实现sigmod；
  yolo11 和 yolov8 有attention模块，要求量化精度比较高；
  量化成int8，attention模块不量化，还是float形式；

  时序动作分类：3dcnn，融合时空特征的2dcnn，取10帧进行分类；
    训练集效果好，现场效果差；数据集上8个都能检测出来，现场8个动作只能检测出几个；

  现在一般是后处理的方式来做，一般是郑州的项目开发人员；
  服务器运维，网络安全；
  gsteramer，模型上不去就进行适配；docker里起python程序；

  qwen-vl，2b或者3b的vlm，用来进行数据标注；
  自动标注平台；saip，现在用sam2进行自动标注，然后人工复核；然后手调；
  一个是自动标注；
  后续可能的：另一个是利用agent的能力，对yolo模型的训练整个流程实现自动化；大模型自己进行迭代；
  qwen3.5 moe 进行sft的训练测试，之前直接拿官方的模型，调prompt来看是否输出的框更好；

张华煜
  过去：人体检测模型
  当前：优化基础模型，优化误报漏报，漏报比较多；
    尝试不同的模型 sam3;
    主要优化奇瑞的项目，用的模型是11nano，有上百台相机，漏报比较多；
    现场发现后直接发给huayu；
    美国，950相机，有漏报，误报；

    主要做的事情就是：
      现场采集数据或发现问题，进行标注，标注数据给到huayu;
      拿到标注数据后重新训练，然后测试，测试完成后再给到现场；
      能加到person_all的就加入，不能的就单独放一个文件夹，进行混合训练；
      检查标注数据，如果少量标注的数据不好，通过labelimg进行标注的调整；否则打回重新标注；

  相关代码；
    yolo代码
    sam3代码
    异常检测代码，用另外一套方法；
    62.2上通过conda环境来训练；

  尝试有什么办法可以实现自动标注；

程振
  昆山项目调研，9月份到现在做昆山项目；
    两个厂家竞争，我们可以用分割算法来做；竞争对手是湾测；他们用tof相机来做；
    检测框会出现误报的问题；
    和海洋和huayu把分割算法yolov8seg部署到相机上，用户不能用工控机；
    问题：
      分割速度不够，用了yolov8seg最小模型后可以符合要求；
    有个两三百台相机，现在进场70台了，上周又进场30台；
    直接在相机上跑；现场人的衣服和背景颜色像，会产生误报；

    检测3万张，分割有六七千张；
    相机视角已经确定了，相机安装已经固定了；
    后处理逻辑，直接写到相机上；
    怎么标注的：labelme用sam2预标注，然后人工微调；
    怎么部署到相机上的：这部分逻辑是谁来写的；海洋在相机部署上做了分割逻辑；
    不同的相机需要使用不同的模型吗？
      70台相机在用不同的模型，用了五六个模型，现在不能做到使用统一的模型；
      目前是双目相机一个目做检测，一个目做分割；

    1，分割模型标注，采集数据，数据标注，数据训练，再部署进行训练；存在视角问题的改动；
    2，模型部署
    3，在不同的相机上需要使用特定的模型；
  
  本地测没问题，现场会有问题，现场误报的数据在本地测，本地测是正常；
  1，嵌入式实时检测的流和ai推理的流不同；


方松 
  和海洋做多模态数据集构建，数据集来源：已有的图盘的yolo标签；图片+文本；
  62.2服务器上的数据进行备份，进行整理；
  saip平台的数据进行清洗，用sam3标注然后和人工比对，重合度底人工来看；
  人工的数据的标签数据有异常；有很多图片的相似度很大，需要去掉很多相似的图像；
  这个数据清洗完之后来给海洋用，通过saip上来挖掘数据；
  现在清洗完之后放到了62.2服务器；还没正式开始；
  目前的任务：整理当前的数据的文档；

  和嘉善的测试人员一起，人工审核7万张图片，sam3标的和人标的，人工看哪个标的好；
  区分之后用来huayu进行基础模型的训练；

曲晨
  郑州负责人：王金峰

  项目：
    昆明 立讯  进行中
      要识别手和标签，郑州马天淄在做；目前是交付人员在搞，不需要算法参与；

    沛顿 
      郑州杨亚董 梁咏琪来做，通过saip平台，标注图片来做；要交付了
      识别3个动作：通过yolobb旋转框来做；做测试；通过docker提供；
      有个问题：重启服务器之后docker不是启动状态；

    广域铭岛&耀能行为分析
      人脸关键点检测不出来，帽子和口罩有影响；
      识别人脸不稳定；
      yolo pose 通过肩膀的关键点和头部的5个关键点；
      这个项目主要是曲晨在做；
    
      标了1000张数据，检测点芯，数据比较重复，效果差；
      郑州豆名健负责，曲晨来帮忙补充算法；
      封装为一个docker，通过mqtt协议进行通信；30g的docker；
      yolopose的代码进行简单的修改；

      svap平台支持使用docker；
      郑州同事在svap平台中再定义规则，通过java来写；

      目前的3个视频没有问题，需要更多测试；

    长春一汽行为
      手套遮挡导致检测不到手中的道具
      怎么根据检测框来定义行为，目前都是郑州的贾智源，通过saip识别道具，然后写java后处理；

      目前的问题是识别不稳定，可行的解决方法是裁剪图像，只对特定区域进行识别，但是现在的svap不支持；
      本地重新训练模型，通过roi检测效果可以改善，需要封装为docker来部署，现在的svap不支持裁剪的操作；

    代码和docker，都在本地的4070电脑上；

  梁，项目情况
    原则上所有的项目郑州都干；
    10个在做的项目；并行来做；一个项目一个调试加一个开发；两个人做一个项目；

    交付周期，快的两三周，慢的两个月，慢的主要是因为目标检测类不太好解决的问题，
    需要算法来优化；
    慢的话主要是指标上不去，他们的解决办法主要是采数据；

    目前 huayu 百奥智能项目 从23年到现在，每年都有一些新增的需求；
      偶尔有新的需求；

    程振 lizhen 安全相机，在双目安全相机上跑一个分割算法；一直在跟这个项目，有半年时间；

    曲晨跟了几个项目，目前郑州解决不了的项目是交给曲晨来再次开发；

实习生：
  石子平
  邓豪
  吴佩秦


刘赞：
  1，数据平台的调研，看能否在设计上进行优化；
  2，自动化数据清洗的方法，大模型相关的方法测试；

当前人员分工
  海洋： 大模型预研 & 微调量化 硬件部署
  华煜： 训练检测人的 yolo基础版本模型  & 佰奥项目支持
  小戴： Deepedge 自动标注训练工具 
  曲晨： 项目算法
  程振： 项目算法（立臻） &  人员分割算法 yolo seg
  方松： 训练/评测数据归类整理 

标准化工作：
  每个人整理自己项目的数据采集标准文档和标注标准文档；根据项目划分；
    在通用检测下加一个项目文档
      项目介绍
        简介
        开始时间
        结束时间
        识别内容与示例图片：
        使用设备：
        训练数据：
        训练代码：
        训练指令：

        主要block项

      数据采集标准-20260601—v1
      标注标准v1

      项目问题解决：
        问题：
        测试过程：
        解决时间：
        解决方法：

1，人体检测模型
2，通用检测模型
3，行为分析模型，视频类任务；

1， sync with qingxin；
  1，熟悉当前的数据平台的开发
  2，后续优化这部分的设计与效果；
  3，之后进行模型方面的探索；
  4，大模型的训练与部署
    https://alidocs.dingtalk.com/i/nodes/OG9lyrgJPLe06Yj3Hz77dQLnJzN67Mw4

