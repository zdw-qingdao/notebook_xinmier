

1，design the ray framework;
  1，所有对ray的使用，排队情况的查看；现在有几个任务在执行，显卡占用的情况；
  2，模型训练
  3，模型推理
  对旋转框的标注和训练支持；

0，任务调度新版实现与测试；
  1, 新版任务调度的迁移；
  2，rebase最新的main分支，进行新的适配；

1，编辑与可视化的方式：
  方案一：一个是workflow设计页面，一个是部署页面
  方案2：workflow设计也面和部署一面一体化；
  选择方案1，部署页面每个部署，可以选择编辑原workflow，也可以选择创建新的workflow并编辑；

2，进程设计细化
  1，workflow执行引擎是单独的进程；
  2，推理引擎也是单独的进程；
  3，单独进程启动方式：通过scheduler任务调度的方式统一启动；可以通过ray的dashboard整体来看；可以复用scheduler的功能；
  4，进程通信的方式：还是通过mqtt即可

8，增加一个相机选择变量，是针对从已有的相机列表中选择相机；
7，flowcontrol的实现方式，也支持内部提前返回；
6，workflow前后端沟通的方式
5，模块内部实现方式；
4，模块内部接口细化
3，模块外侧接口细化，通信接口细化

任务调度部分设计，实现与适配；

先不做这个修改，目前这样也行：train log，只显示log内容，不显示全部内容，全部内容可以下载log文件，增加个链接；

统一采用一种通信的方式；要支持多机多进程通信；

1，通信方式技术选型；目前svap和相机是采用的mqtt；
  采用中间层加mqtt的方式，如果后续需要修改修改实现层；
  mqtt没发现什么问题，后续还是采用这样的方式；
  deepstream执行引擎是不是可以直接拿过来；

1，视频流推理支持
  1，模型侧：
    视频流，通过deepstream进行解码和推理，将结果通过mqtt pub； 
  2，通过task封装；
  3，前端显示适配；
    rtsp视频流+mqtt pub的内容；
  在不支持deepstream的情况下，先实现这部分，调用推理结果进行推理，获取推理结果并显示；
  支持deepstream的情况下，加入deepstream的推理引擎，然后获取mqtt推理结果；
  需要做的是deepstream的推理引擎；

1,海洋先做这个；
  相机模型推理引擎，svap是怎么把模型发送给相机的；2.0先复用同样的方式；

1，svap中推理引擎的逻辑：
  inference_engines 里是推理引擎
  yolo yoloobb 等分别是不同的进程
  每来一个需求就开一个线程，一个pipeline来处理；
  输入输出都是走mqtt；

workflow资源调度系统设计：
  2，workflow执行引擎资源调度的方式；
  1，推理引擎资源调度的方式；

同步ai coding开发理念
  学习，加深理解的过程
  同步标准的过程
  最终哪个好用哪个，如果20分钟改进版更好，那就用更好的，prompt是共享；

1，designer的使用逻辑；整个使用与执行过程；
  designer得到的是什么文件
  执行方式是什么；

1，支持的相机：
  rtsp://192.168.30.52:554/live
  rtsp://192.168.10.240:554/live

第一版workflow设计完成；

删除操作修改：
  1, reference 确认下，是否都不需要了；
  2，确认下图片复制一份，是存放到什么位置，补充到data_plan.md 中
  3，index，使用图片主键；

获取信息的方式；通过统一的数据库，前端需要获取的信息，都存在部署表中；

4，推理调度是否需要单独进程？不需要，后续需要的话再开；

机器：             15066 
  8卡l20
  500g系统盘 
  内存1024G
  cpu Intel Xeon Platinum 8563Cprocessor 2.6GHz  208核
200M上下行带宽     5974
cfs容量型20T       3269
月总价24309

5，模型预览推理可以单独起进程；没必要和推理引擎放到一起；

4，数据集上传进度，显示进度和速度即可，不需要设置为任务；

3，当前的搜索；是搜索到就加到相机表；可以加入到任务调度中；都可以，必要性不强；

2，每个用户当前的任务显示，有个悬浮窗可以显示；
  当前用户的任务显示，通过悬浮窗来展示
  当前用户的训练和排队情况；如果在排队，显示前面有几个任务在排队；

5，62.2也进行部署，多机多节点算力支持；效果展示；

1，sync with haiyang;

todo：
  1，深度测试
  2，workflow新版完成
  3，视频流推理支持；

5，进行训练测试；

4，模型一键导入相机；通过平台配置相机参数；

3，新版workflow方案设计完成，后续支持 workflow 在相机上跑模型，在pc上跑后处理；或者在pc上跑模型+后处理；预计1-2周开发初版；

2，windows安装包；支持windows安装系统，支持老版本windows;
  只支持win11；

1，任务调度更新，当前系统需要，可以支持灵活外接算力；调整任务优先级，内部任务启动比较容易；

1, saip数据一键上传数据平台的功能，需要指定ip，用户名，密码；
  wangyang;

2，slam任务，yinsheng；

百度云使用方式：
  1，每个人用自己账号；
  2，使用指定账号来正式启动系统；

2，叉车盒子+相机，找黄，通过slam的方法判断前进还是后退； yinsheng
  1，服务器上做个初版，通过前进后退来进行一次标定；
  2，部署到端侧，测试效果；
  我先来设计方案，然后quchen来进行实现
  服务器上直接进行初版的实现，quchen来进行部署测试；

1，百度云部署：
  5, autopipe账号启动正式版本后续再搞
  4，启动测试，在 zhongdawei账号；              
  3，代码拉取，在 zhongdawei账号；              done
  2，docker环境；通过cursor                    done
  1，翻墙配置；通过cursor，在zhongdawei账号；    done

1，海洋代码review
  1，create_rknn_model_package 单开进程，没必要优化，这样就可以正常执行；如果优化的话可以通过ray来执行python环境；
  0，data_plan.md 中缺少两个字段；

1，可视化设计

linli work: 
  0，linux部署脚本；
  1，linux的更新，有一个更新页面，有网自动拉取，没网就手动拖拽文件，然后手动点击更新系统；
  2，windows支持后续用wsl2的方式；如果有需要再考虑具体安装的方式，否则停止这部分开发，已有改动保留，新的改动不合入；
  
项目会，同步codex报销的问题：统一使用财务这边的信用卡；

3，engine.py 的执行逻辑，细节调整，代码结构；

1, postgre 数据库的存放位置修改；

1，复现当前删除的问题，解决这个删除的问题；项目删除的问题已解决；

2，删除dataset，项目模型中会报错，目前修改为如果存在模型引用，那么不允许删除

1，运行log输出到文件；log保存的位置；直接使用 supervisord 的方式；通过单独的进程来管理log文件；

3，启动失败的问题查看，优化提示逻辑，如果启动失败马上提示原因；

3，训练任务1张卡，推理任务卡的数量，是否合理；看卡的数量选择是否合理；
  不同大小的模型，不同的卡的数量和batch约束；
  训练任务，只允许选择整数张卡；否则显存不够会出现冲突问题；

6, 数据库存储方案，使用本地的云磁盘；看是否要加购磁盘；目前暂时不需要；

2，workflow manager的实现，review；

3, haiyang task;
  1, 同步的问题；在校准阶段根据时间戳同步，同步一次就可以，这样不需要手动设置参数；
  2, 上传图片padding成0，排序看起来正常；
  3，有没有必要通过ray的run直接获取返回结果； 没有必要，先这样也可以，反正功能一样；
  4，service 中的代码整理，auto_label的一个文件夹，preview_inference 的一个文件夹；

1，测试视频能否正常使用，如果不能正常使用bug是什么；
  看62.9上的部署为什么不能正常使用；看视频需要的有哪些；同步调整百度云；注意百度云要打开防火墙；

1，首先加一个标志位，默认是false，然后在推理的时候，如果是true，那么要么现在执行，要么不执行，不会加到队列中

1，资源分配的设计；合理的接口方式；
  要么提交指定类型的任务，taskservice这里读取配置文件，来分配资源用量；
  要么指定资源用量；
  task_resource_type;

zhongdawei@120.48.26.246
代码路径：/cfs/users/zdw/code/autopipe2/autopipe
已通过 docker_run.sh 启动: http://120.48.26.246:11700/workspace/1/cameras
为什么不能正常看rtsp视频流: 需要百度云开放 UDP 端口；

  浏览器不是直接播放 RTSP，而是通过 WebRTC/WHEP 预览。按当前配置，防火墙需要开放：
  TCP 23710：前端页面
  UDP 8182：WebRTC 视频媒体传输，最关键


1，62.9 测试视频流不能正常预览，过了一段时间后会关闭，不能通过选择预览重新连接；
  haowei：
    /home/zhongdawei/code/autopipe2/autopipe
    通过 docker_run.sh 正常启动了系统:http://122.225.62.9:23710

1，62.9 百度云 视频流显示的问题
  /home/zhongdawei/code/autopipe2/autopipe，通过 docker_run.sh 正常启动了系统，这里显示的视频流
  现在显示正常了；

1，输出topic有一套单独的逻辑，mqtt_input_name 是 workflow更新后的输入字段的名称


codex报销问题；


2，百度云部署

1，数据store优化


