




4，使用claude 和 codex；先使用我的账号，然后公司购买；
3，平台化
2，数据平台；首先数据需要备份，实现数据闭环；  
  网页管理：
  2， 数据


3 降低返工次数，
 提升模型能力，提升评测能力，判断模型边界
 提升数据管理的能力


4， 完善的部署能力；部署到不同设备，不同工厂，不同工位相机；
3， 提升检测模型与标注模型的能力
2， 评测能力
1， 过程标准化

2 ai coding 
3 代码平台
1，数据和训练平台

1 整理当前的问题
  1，没有代码管理
    训练代码，工具代码是散的；
    在某些soc上的算子开发是散的，没有集中管理；
      可以放到一个统一的库中；这样可以互相借鉴，别人也可以看，可以修改；
    
  2，数据管理较乱，数据安全差；通过云端管理；

  3，设计平台和嵌入式的工作，需要我们来参与
    svap平台不支持一些模型，需要另外起docker；适配性不够好；
    详细看svap平台的实现，调研相关技术，然后看怎么改进；
    相机后端看实现方法；具体来看；

  4，目前安全场景部署慢的问题：
    存在漏报误报多次返工；
      1，数据采集与标注需要调整；
        
        0，数据采集要有明确的标准；尽量涵盖所有corner case；
          同一个工位需要多个人进行操作，需要在不同的光线条件进行采集；
          也需要文档化，这样其他人可以优化，也可以进行迭代；
          避免手动调整标注，标注效果作为标注人员的绩效，需要标注人员主动和我们对齐标注标准；
          标注结果复核还是我们来进行，看是因为标准没有明确清楚，还是明确了但是没有标好；如果是后者，那么记录数量；

          自动化标注是很重要的，不过工厂场景比较复杂，我们始终要保留人工标注的能力；

        1， 标注标准需要明确；要有严格的人工检查；
          标注标准需要能够复用，所以后续的标准要文档化；
          如果标注发现问题，通过文档来和标注人员进行迭代；后续也可以优化文档；

      2，提升标注模型的能力，
        1， 提升标注模型的评测能力；
        2， 标注模型优化：
          1， 测试大模型
          2， 迭代finetune的能力，
            可以利用采集的数据训练模型，然后再进行标注；一个迭代的过程；
          后续全部在云端实现；可以选择不同的模型进行自动标注；
  
      2，模型能力不足；缺乏评测能力；不知道不同模型的泛华性能；
        现在都是训练完后在评测集效果不错，但是在现场存在泛华能力不足的问题；
        需要临时补充数据

      本地要尽可能发现当前问题，减少返工的次数，现在本地validition通过不能说明问题；
      要有明确的评测pipeline；

      4，不同工位可能存在较大的差别，不同工位使用不同模型的问题
        在数据采集入库的时候要记录不同的工位，在训练的时候可以指定通过所有数据训练，或者通过所有数据训练后再在自己的工位上单独训练，这样得到每个工位的模型，在模型部署的时候部署
        
        具身的跨本体问题：  
        1，单独数据进行finetune；
        2，训练的时候不同的相机输入不同的id；
        目前都是用方法1和2结合，发挥效果以方法1为主，所以我们优先使用方法1；

      5，模型部署非自动化：
        目前的模型部署是人工发模型，后续统一在远端指定某个工位某个相机用哪个模型；
        现场人员直接通过云端平台拿模型或自动部署，我们不需要手动来发；

      6，数据回流的媒介：通过网络上传；或需要手动传到一台机器，然后通过网络上传；
        1，saip平台数据上传
        2，人工拷贝微信或钉钉发送；
        后续统一上传到云端平台，上传方式由交付来实现，

      7，数据问题：

      后续：
        1，数据采集标准化
        2，标注标准化
        3，训练评测标准化
        交付之后，如果出现问题，每个问题要有记录，看是上面哪一步的问题；
        最后希望达到的效果是交付后不返工；

https://alidocs.dingtalk.com/i/nodes/0eMKjyp81M6EnlORC4b1MyMm8xAZB1Gv

        




