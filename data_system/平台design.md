

1，当前问题：
  0，缺少系统化评测模型能力的方式；
  1，服务器运维问题，数据安全性差；缺少备份；
  2，项目执行过程缺少记录；
  3，saip是windows应用，更新需要安装新版本的exe，不如web方便；
  4，标注结果不支持多个版本；
  5，缺少版本管理：训练结果和对应的评测结果；也就是缺少发版的流程；
  6，不支持对指定工位的数据进行finetune；

2，最终版本是什么
  数据，数据支持不同的类型，针对不同的任务；
  标注，可以选择使用什么模型，标注区分不同版本；可以直接手动调整；可以复制一个版本然后手动调整；
  模型，对应到bitbucket的commit;
  模型文件，不同版本，对应训练数据，训练模型；
  任务进度，训练的执行过程；
  部署情况：
  评测方式：可以指定不同的评测方式，评测指定的模型，可以批量评测；
  算法方面项目管理
    一个项目
      对应某些数据
      对应某些训练
      项目数据补充
      项目标注调整；
      每次训练对应的问题；
      通过web界面可以看到整个项目的迭代过程，可以看每个相机的时间block项在哪里；

3，版本迭代过程与时间节点：
  1，火山云开通与使用
    包括docker管理；

  先基于62.2进行开发； 后续迁移到火山云；可以取消备份操作；

  2，数据管理测试，通过脚本执行
  3，模型管理与训练测试，通过脚本执行
  4，前端开发与脚本对齐
  5，评测系统开发
  6，标注系统开发
  7，项目管理设计

