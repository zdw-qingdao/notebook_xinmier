

design, the final version and the middle version of the system.
  7, 长期：面向对象设计，全部web操作，逐步迁移到供应商操作；从自身快速部署到供应商快速部署

  6, 短期：面向过程设计，流程化清晰，加快过程效率；
    面向对象设计，全部web操作；逐步迁移到供应商操作；
    云端web或本地web，对应的数据server可以是云端或本地；后续全部迁移到云端，通过供应商操作；
      标注系统
      集中数据管
      模型训练系统
      评测系统
      部署系统；
      端侧数据管理；

svap平台的实现；
相机后端代码查看；

2，搞个habour docker，来挂各个docker，后续大家用这些docker来运行代码；海洋来搞一下；

----------------------------------- 
  1, the platform
    wangyang
    huayu
    fangsong
    liuzan

  2, the data quality
    fangsong
    huayu
    
  3, the detection model
    huayu
    chengzhen
    quchen

  4, the behavior model
    haiyang
    传统视频分类模型
    大模型方法

  5, large model 
    haiyang 
    haowei

  6, project
    quchen


目标拆解：
● 传统人体检测模型（6月底发版）：
    ○ 用于简单安全类任务（遮挡少，人物特征清晰）；
    ○ 使用 YOLO 系列；

    打签数据训练系统，修改不同数据的采样频率；看是修改 data.yaml 还是dataloader
    通过数据平台完成数据清洗，数据分类，数据打签操作；

● 多类别识别模型（7月中旬发版）：
    ○ 用于复杂安全类任务（遮挡较多，人物特征不足）；
    ○ 覆盖人、手、椅子、衣服、安全帽、水渍等类别；
    ○ 使用 YOLO 系列；
    ○ 重点考虑剪枝、量化、蒸馏和高分辨率适配。

    saip全部数据进行数据清洗和tag分类；

● 通用检测模型（八月中旬发版）：
    ○ 用于行为类检测和部分较为复杂的安全类任务；
    ○ 明确 YOLOE 作为主线研发方向；

    行为类数据调研与管理；

● VLM（预计10月发版）：
    ○ 作为通用行为分析的中长期重点方向；
    ○ 后续研发精力仍需重点放在 VLM，因为架构更通用。

yolo模型迭代;
大模型迭代;


数据迭代：
  1，增加数据；
  2，数据清洗；
  3，数据打签；


发版流程，分为数据的迭代和模型的迭代；
  1，预训练
    开源数据 + 所有现场数据 + 分类的现场数据
  2，后训练
    现场采集的数据*3 + 分类的现场数据；

模型评测：
  1，要评测哪些内容：
    1，评测集具体指标，具体到不同的tag；
    2，评测集图像对比；
    3，loss曲线等
    4, 推理效率；

  2，确定评测集
    1，所有的图像抽出来一部分作为评测集，其中根据不同tag来看泛化能力；

  3，后端与前端设计：
    1，后端部分：
      1，指标存到json文件，输出具体的tag推测；针对每种tag记录评测指标；
    
    2，前端部分
      模型结果页面：
      1，自动显示一个val.json中的内容，通过一个表格来显示；
      2，如果有inference的部分，可以查看inference图像或video;
      3，可以筛选推理结果不好的部分来单独查看

    3，模型对比
      1，选中要对比的一个或多个模型；
      2，通过表格同时显示 val.json 文件中字段相同的部分；
      3，可以选择图片进行对比：
        1，结果输出图片；
        2，inference图片；
        3，video拼接显示；


4，后续数据存储方式，通过json文件来存储；
  1，全部转成json文件
  2，数据标注的读取和保存改为json文件
  3，数据读取转为json文件；
  4，数据处理也转为json文件；

  需要整体调整，收集所有需要修改的地方，然后按顺序修改
    1，当前数据整体修改
    2，页面显示和标注修改
    3，yolo数据读取修改
    4，数据处理转为json文件

这个目前不重要：
  11，数据处理适配
    1，sam3标注，设置保存格式
    2，大模型打签，针对打签的标注版本的格式；
    3，数据去重适配json文件的格式，可以对不同类型的标注进行去重；针对标注的格式；





