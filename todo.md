
6，数据平台json文件适配的问题，看是否每一部分都进行了适配；华煜 linli fangsong 林立 需要确定责任人；

4，说明书，整个的执行过程；分为几种不同的情况；
  web训练+web部署
  web训练+现场部署
  现场训练+现场部署；
  有网dockerhub；没网通过u盘；

后续：
  1，现场docker的搭建；怎么在windows电脑上搭建；
  2，用户名称，workspace名称重复的问题；
  3，dataset 名称重复的问题
  4，model 名称重复的问题
  参考数据保存，数据保存通过文件名称分开，名称不能重复；
  5，数据版本管理；
    1，创建数据集的时候，可以选择把标注和图片路径cache下来；生成cache文件，训练可以直接读取cache文件；在训练的时候，如果有cache文件那么使用cache，如果没有cache那么实时读取；

7，删除项目
  包括删除权限的引用；
  包括模型表的删除；

5，测试下目前的dataload时间和cache方法的差异

zenmux报销；
水壶

6，agent实现
  先进行设计，端口预留，交互逻辑；

5，用户登录，用户权限，收费方式的整体设计；
  1, 管理员界面可以配置用户权限，包括workspace的权限，project的权限，model的权限；

4，ray MediaMTX 的 dashboard
  1，如果是admin，要显示dashboard；
    1，资源占用情况
    2，ray任务调度的情况
    3，视频流情况
    4，部署情况；

3，workflow通过任务调度封装一层；

6，厦门项目的测试工件，不需要，拿到硬件实验室；

zejin：
  1，离线客户端的编译过程
  2，代码适配的方式
  3，模型调用的方式；单独写一套？使用sam2或者yolo；

5，多路运行测试；可视化开发与测试；

2，haiyang进行workflow测试，细节调整；linli进行前端接入；
  规定好接口；

1，review架构设计方案，zejin haiyang linli
  sync with linli, zejin，wangyang, how to use the video stream.
  1，其他部分获取rtsp视频流的方法，怎么和相机列表的部分联动；
    方案1：所有的视频流都通过相机列表获取，连接，其他的部分只能选择已连接的视频流；
    方案2：后续除了已连接的视频流外，可以另外指定其他视频流；先采用方案1即可；
  0，workflow的后端，前端调用后端的方法；

7，背景优化

6，前端对齐：
  1，模型展示部分 zejin

5，模型推理block，需要有模型文件转换；
  后端 haiyang
  前端 林立
4，linli，前后端接口；
  1，使用workflow5；
  2，flowcontrol block;
3，保存结果查看与下载；
1，模型结果展示部分查看效果；

下周三开始：
嘉善和郑州使用新平台进行测试；测试模型训练的部分

测试前需要准备：
2，常态部署docker和数据库docker，单独的数据文件夹和端口；目前只分配2张卡；训练的时候分配两张卡；
1，模型训练和推理的部分接入ray；

用户计费逻辑设计与实现；资源点的消耗方式；
  消耗行为：
    数据上传
    保存
    模型训练
    使用store中的数据或模型


5，workflow部署部分：
  3，视频流测试；
  输入形式说明
  数据流转形式说明
  batch输入支持
  deepstream支持
  端侧部署支持，预留接口；
  数据保存block实现
  可视化block实现；

周一：
sync with haiyang：
  4，模型训练和推理都走ray来执行，确认单机多卡可以正常运行；
  3，main
    sync_repo_models_to_data_path
  2，sam3的权重放在哪；
  1, model 存放位置的问题；
  0, 权重存在一个单独的文件夹，挂载到docker镜像中；后续直接打包到docker内；

sync with zejin：
  2，权限管理设计，权限管理界面；

       
公开数据集 公开模型集 图片删除 逻辑：
  1，数据库调整：
    1，图片表加project_id；
    2，公开数据集表
    3，公开数据集权限表，指定哪个用户对哪个数据集有fork权限；
    4，公开模型表
    5，公开模型权限表；指定哪个用户可以使用哪个公开模型；

  2，创建公开数据集的操作：
    1，管理员可以将某个数据集设置为public，即将这个数据集加入公开数据集表；这时候需要指定数据集描述，可以选择添加一个头像图片；
    图片在 /public/dataset/xxx/info.jpg
    指定该数据是所有用户默认有权限，还是需要指定权限；如果需要指定权限需要在公开数据集权限表中添加权限；即公开数据集表中有一个权限类型；
    2，管理员可以查看 编辑 删除 公开数据集；

  3，导入数据的时候，有一个公开数据集选项，点击后跳转到公开数据集store，可以选择某个有权限的公开数据集fork；会显示公开数据集的介绍和头像；

  4，fork操作对应的是创建新的图像表和标注表，标注类型使用当前默认的标注类型；

  5，删除逻辑：在图片表中删除这一项，在关联的标注表中也进行删除；

  6，不会有干扰：admin创建数据集，然后设置为公开数据集，用户a fork，admin/用户a 删除图片，都是操作自己的图像表，不会造成干扰；

  7，管理员可以将某个模型设置为public，即将这个数据集加入公开模型表；这时候需要指定模型描述，可以选择添加一个头像图片；
    图片在 /public/model/xxx/info.jpg
    2，管理员可以查看 编辑 删除 公开数据集；

  8，在模型列表，有一个公开模型集选项，点击后跳转到公开模型集store，可以fork模型过来，
    1，fork模型后，在模型列表显示这个模型；
    2，fork模型后，同样可以直接基于这个模型创建workflow，也可以用这个模型作为下一次训练的预训练模型；也可以查看这个模型的训练结果，进行inference查看；

权限管理方式：
  1，workspace权限表：
    1，workspace创建者可以邀请其他用户；
  2，权限管理界面，如果是管理员登录，可以选择已有用户，增加workspace权限，数据集权限，模型权限；



