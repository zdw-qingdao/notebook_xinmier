
4，说明书，整个的执行过程：
  现场采集数据+web训练+现场部署
  现场采集数据+现场训练+现场部署；
  通过u盘安装系统；

后续：
  1，现场docker的搭建；怎么在windows电脑上搭建；
  2，用户名称，workspace名称重复的问题；
  3，dataset 名称重复的问题
  4，model 名称重复的问题
  参考数据保存，数据保存通过文件名称分开，名称不能重复；
  5，数据版本管理；
    1，创建数据集的时候，可以选择把标注和图片路径cache下来；生成cache文件，训练可以直接读取cache文件；在训练的时候，如果有cache文件那么使用cache，如果没有cache那么实时读取；

5，测试下目前的dataload时间和cache方法的差异

5，用户登录，用户权限，收费方式的整体设计；
  1, 管理员界面可以配置用户权限，包括workspace的权限，project的权限，model的权限；

zejin：
  1，离线客户端的编译过程

8，用户计费逻辑设计与实现；资源点的消耗方式；
  消耗行为：
    数据上传
    保存
    模型训练
    使用store中的数据或模型

6，workflow部署部分：
  数据保存block实现，保存图片；
  定时执行单次的逻辑；
  deepstream缓存10秒视频的逻辑，触发时保存；
  可视化block实现；
  显示的内容：
    1，debug block，完全自定义显示，在cpu上进行图像绘制；
    2，通用的单张图片显示变量，通过这个类型的变量进行显示；自定义draw函数；
      浏览器上在rtsp上叠加显示的方式，这样的问题是可能不同步；

# ------------------- 
baoxiao issue；

1，实现监督模型的部分；

# ----------- 开始测试

1，自定义可视化看板，灯，button按钮等；

------------------------------------------

预览直接接相机上的webrtc；显示延迟的问题；
如果是通过rtsp视频流，显示有延迟，相机前端是直接接的相机上的视频流；可以直接拿这个来显示；

--------------------

7，分工与排期
  linli   前端进行workflow部署，可视化，故障显示；         
  1，后续可以考虑将workflow执行引擎也放到相机上，可以支持在相机上跑全部，这个后续有需要再说；
  2，生态补充，后续可以大模型批量进行，需要写好prompt；
    可以intership慢慢补充；正式来审核；

5，先验证是否存在问题；haiyang 优化；
  1，不同的标注类型，测试yolopose，yoloobb的效果；具体的支持上有没有问题；

  2，启动的判断端口 ip 是否正常，如果不正常在docker_run的时候就提示；

0, linli:
  1，安装部署也需要用同样的方式；log文件保存到指定的位置；定时清理；

------------------

zejin:
  3，9月1号前，郑州使用：
    1, 收费方式 credit 开发；
    2，离线部署；license；

wangyang:
  1, 离线saip的数据上传2.0平台；模型和权重；

haiyang:
  1，推理的部分显示不正常；

2，haiyang cl review;






